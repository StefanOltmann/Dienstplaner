import org.gradle.plugins.ide.eclipse.model.AccessRule

allprojects {

    group = 'de.stefan-oltmann.dienstplaner'
    version = '0.0.1-alpha'
}

subprojects {

    apply plugin: 'java'

    repositories {
        mavenCentral()
        jcenter()
    }
}